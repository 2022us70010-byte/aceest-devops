apiVersion: v1
kind: Service
metadata:
  name: aceest-bg-service
spec:
  selector:
    app: aceest-fitness
    version: blue        # change to 'green' to switch traffic
  ports:
  - port: 80
    targetPort: 5000
  type: NodePort